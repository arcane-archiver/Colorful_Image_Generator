#ifndef COLORFULIMAGEGENERATOR_HPP
#define COLORFULIMAGEGENERATOR_HPP

#include "../cs225/PNG.h"

namespace ColorfulImageGenerator {
  cs225::PNG generateImage(unsigned int width, unsigned int height);
}

#endif